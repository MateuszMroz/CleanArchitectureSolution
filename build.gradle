buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath libs.android.gradle
        classpath libs.kotlin.gradle
        classpath libs.junit5.extension
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
